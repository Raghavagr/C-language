Take name and salary of two employees as input from the user and write them to a text file in the following format:
        name1, 3300

        name2, 7700
        
        
 //code
 
 #include <stdio.h>

int main() {
    File *ptr1;
    char name[30];
    float sal;
    
    ptr = fopen("emp.txt","w");
    printf("\nenter emp_name");
    fgets(name);
    printf("\nenter salary of emp");
    scanf("%f",&sal);
    
    fprintf(ptr,"%s, %f",name,sal);
    fclose(ptr);
}
